include partials/head.pug

main.ctr-flx                                
    section(class="container txt-ctr")
        h1 404
        p The page you are looking for does not exist.
        p Go back to the 
            a(href=paths.GENERAL.home) Home

include partials/footer.pug